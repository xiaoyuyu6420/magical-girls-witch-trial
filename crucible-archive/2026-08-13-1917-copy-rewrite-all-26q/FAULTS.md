# FAULTS.md — 流水线失败/债务日志

## F1 · 预存测试债务（非本次回归，2026-08-11 记录）

**文件**：`src/data/quiz-content.scores.test.ts`，3 个断言失败

**原因**：断言引用的是**上次清空文案前的旧 label 文本**，文案清空重写后未同步更新：
- L18 `q.meta === "羁绊·邂逅"`（Q3）找 "第一个没谈条件的善意"——Q3 label 已重写为"哪有/被你说中/墙是该竖的"
- L25 `q.meta === "评判·停步"`（Q13）找 "我说到做到"——Q13 label 全空（反向批次待写）
- L32 `q.meta === "执念·崩塌"`（Q20）找 "心里什么都没了"——Q20 label 全空（反向批次待写）

**判定**：预存债务，非金标打磨 + 排版增强（本次改动只动 text 题干，未碰 label）引入。

**处置**：延后到 Q13/Q20 label 写完批次时统一更新断言文本（断言本意是"score 与语义对应、防位置绑定"，有价值，保留逻辑换新 label 文本即可）。当前阶段不阻塞。

**影响**：契约 R11"测试套件全绿"暂不满足——审计前必须清零。
