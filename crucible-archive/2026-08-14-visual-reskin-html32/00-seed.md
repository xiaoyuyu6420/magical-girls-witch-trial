# 00-seed · HTML 32 视觉重做（答题页金紫黑赛博哥特 + 结果页塔罗分栏重构）

> 不可变锚点：原始目标 + spec 来源 + 用户决策。任何人（含审计者）查"最初被要求做什么"以此为准。
> 创建时间：2026-08-13

## 原始目标（用户原话 + 上下文）

1. **触发**：用户提供了新 HTML 原型 `/Users/munich/Downloads/ai_studio_code (32).html`（"审判庭·灵魂裁决与魔女降临"），sha256 前 12 位 `2ea4adf6a9cf`。
2. **核心诉求**：
   - "你做的手机版答题页面和这个 html 并不一样，我要求**样式细节动效都完全一样**"（指 HTML 32）
   - "结果页面没有设计感，桌面版问题太大，显示不全，**分栏**吧，手机版排版太差，这个页面直接**重构**"
3. **两条硬约束**（本次会话追加）：
   - "没有完成上述计划不准结束"（goal 循环语义——跑到 done）
   - "除了计划中的修改，**不准有细节上的损失**"（计划允许的视觉改动之外，现有功能/细节不得意外降级）

## 用户已锁定的三条方向决策（AskUserQuestion 确认）

| 维度 | 决策 | 说明 |
|------|------|------|
| 桌面答题动效 | **保留挤压动效** | 桌面普通题保留 flex 挤压（1→1.8→8），不换成 32 的 3D Tilt；只升级配色到金紫黑 + 补 32 的背景层（Canvas 粒子/浮雕字/极光转场）。手机端完全复刻 32。 |
| 天平(Q8/Q22)/筹码(Q14) | **保留特殊视觉，只改配色** | 保留之前做的 SVG 真实天平 + 点阵点击视觉，把配色从香槟(`#d8c8b0`)改成金紫黑。**不**回退成 32 的普通选项/+/- 步进器。 |
| 结果页立绘 | **用 32 的占位符** | 五角星 SVG + 角色名占位（32 原样），后续有图再换。 |

## 关键事实（HTML 32 vs 当前实现的差距，探查确认）

### 答题页
- **配色**：当前双套（桌面金紫黑 `#d4af37`/`#8b0000` + 移动香槟 `#d8c8b0`）；32 是**金紫黑赛博哥特**：`#FFD700` 金 / `#D800FF` 霓虹紫 / `#7B2CBF` 深紫 / `#FF0055` 玫瑰红 / `#030305` 虚空黑。
- **32 有、当前没有的招牌层**：Canvas 紫色粒子背景（45 粒子 rAF）、巨型浮雕字 "JUDGMENT" + 题号、极光爆裂转场（aurora-burst，答题结束→结果）、HUD 双胶囊顶栏（左返回+计数 / 右重启）。
- **天平/筹码**：当前是 SVG 天平 + 点阵（按决策保留，改配色）；32 原版是普通选项 + 步进器（**不采用**）。
- **普通题序号**：32 是 "01/02/03"（Syne 字体）；当前桌面是 "I/II/III"（Cinzel），移动端用 Syne。
- **字体**：Syne / Cinzel / Noto Serif SC 三个已全部加载（layout.tsx 已就绪）。

### 结果页
- **当前**：单栏 640px 居中 + **米白浅色主题** `rgba(250,250,250,0.85)`（与答题页深色割裂）；`height:100vh` 不滚动 → 桌面内容超长被裁切（"显示不全"根因）；纯文字无立绘；死的两栏 CSS（`.r-left/.r-right`）从未被 TSX 使用。
- **32 目标**：左右分栏（≥850px：左 380px 塔罗立绘框 3:4 + 右 1fr 详情）+ 金紫黑深色（`#030305` 底）+ 渐变标题（白→金→紫）+ tag-cloud chips + verdict 卡片（审判官逼视/温柔落点）+ posture A/B/C **霓虹柱图** + 操作按钮（金紫渐变主按钮 + 透明次按钮）。

## Spec 来源（自动发现）

- **主参照（逐像素金标）**：`/Users/munich/Downloads/ai_studio_code (32).html`（sha256 `2ea4adf6a9cf`）—— 答题页 + 结果页的样式/动效/DOM/配色全部以此为准。
- **计划文件**：`.zcode/plans/plan-sess_b1722a51-cd67-47da-8d30-5b08144626bb.md`（sha256 `7f4c363511a3`）—— 之前 plan mode 的四阶段计划，已含三决策。
- **对话上下文**：三决策 + 两硬约束（见上）。

## 路由判定（在 STATE.md）

- level: **T3**（多模块：答题页 TestScreen+globals.css 配色/背景层/动效、天平筹码配色、结果页 ResultScreen 完全重构、测试套件更新；高风险视觉重构；用户要求"不准有细节损失"=强回归保护）
- type: **build**（主要交付是新视觉实现），带 **refactor 纪律**（数据/评分/5角色/26题不变，测试套件保护，审计意图对齐="功能仍是原来那个"）。

## 当前项目状态（2026-08-13 现场确认）

- ✅ 数据层已完成（之前 session）：26 题 HTML 文案、5 角色（晓美焰/鹿目圆/美树沙耶香/佐仓杏子/樱羽艾玛）、posture(A/B/C)+path+tendency+keyUnlocked 评分、match 匹配。
- ✅ 测试全绿：vitest 36/36，E2E 117/117。
- ⚠️ 答题页是**香槟暗黑**风格（基于 31.html），需要按 32 改成金紫黑赛博哥特。
- ⚠️ 结果页是**单栏米白**，需要完全重构为 32 的塔罗分栏金紫黑。
- iframe 架构：`public/index.html`（宿主）+ `/test`（答题 iframe，TestScreen+ResultScreen 在此挂载）。背景层（canvas 粒子/浮雕字/极光转场）加在 iframe 内。
